- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: nvim/init.vim

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]